/** Client-safe clients barrel: types only (client adapters live on the server). */
export type {
  AddTorrentPayload,
  AddTorrentResult,
  ClientConnectionConfig,
  TorrentClientType,
} from "./types";
