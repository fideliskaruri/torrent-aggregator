/** Client-safe browse barrel: types only (the rails builder lives on the server). */
export type {
  AvailabilityState,
  Availability,
  ProgressUpdateBody,
  ProgressEntry,
  RailItem,
  Rail,
  BrowsePayload,
} from "./types";

export { COMPLETION_THRESHOLD } from "./types";
